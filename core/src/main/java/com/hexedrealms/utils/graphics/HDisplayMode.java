package com.hexedrealms.utils.graphics;

import com.badlogic.gdx.Graphics;

public class HDisplayMode extends Graphics.DisplayMode {

    public HDisplayMode(int width, int height, int refreshRate, int bitsPerPixel) {
        super(width, height, refreshRate, bitsPerPixel);
    }
}
